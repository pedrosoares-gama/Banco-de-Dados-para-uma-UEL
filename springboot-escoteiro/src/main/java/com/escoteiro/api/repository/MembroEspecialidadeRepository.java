package com.escoteiro.api.repository;

import com.escoteiro.api.model.MembroEspecialidade;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MembroEspecialidadeRepository extends JpaRepository<MembroEspecialidade, Integer> {
}
