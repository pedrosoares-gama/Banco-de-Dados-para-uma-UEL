package com.escoteiro.api.repository;

import com.escoteiro.api.model.Patrulha;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PatrulhaRepository extends JpaRepository<Patrulha, Integer> {
}
